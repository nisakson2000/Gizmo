name: Sync wiki/ to GitHub Wiki

on:
  push:
    branches: [main]
    paths: ['.github/wiki/**']

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Checkout wiki
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}.wiki
          path: wiki-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Copy and rename files
        run: |
          cp .github/wiki/architecture.md    wiki-repo/Architecture.md
          cp .github/wiki/development.md     wiki-repo/Development.md
          cp .github/wiki/how-ai-works.md    wiki-repo/How-the-AI-Works.md
          cp .github/wiki/model-reference.md wiki-repo/Model-Reference.md
          cp .github/wiki/setup.md           wiki-repo/Setup.md
          cp .github/wiki/usage.md           wiki-repo/Usage.md
          cp .github/wiki/_Sidebar.md        wiki-repo/_Sidebar.md 2>/dev/null || true
          cp .github/wiki/Home.md              wiki-repo/Home.md 2>/dev/null || true

      - name: Push if changed
        run: |
          cd wiki-repo
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No wiki changes to sync"
          else
            git commit -m "Auto-sync from wiki/ in main repo"
            git push
          fi
